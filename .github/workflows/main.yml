name: Build

on:
  pull_request:
    paths:
      - ".github/workflows/main.yml"
      - "flake.lock"
      - "flake.nix"
      - "cabal.project"
      - "hie.yaml"
      - "scripts/**"
      - "src/**"
      - "ssg/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/main.yml"
      - "flake.lock"
      - "flake.nix"
      - "cabal.project"
      - "hie.yaml"
      - "scripts/**"
      - "src/**"
      - "ssg/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          extra_nix_config: |
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
            allow-import-from-derivation = true
            auto-optimise-store = true
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org https://cache.iog.io
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= hydra.iohk.io:f/Ea+s+dFdN+3Y/G+FDgSq+a5NEWhJGzdjvKNGv0/EQ=

      - name: Cache Nix store
        id: nix-cache
        uses: nix-community/cache-nix-action@v7
        with:
          primary-key: nix-${{ runner.os }}-${{ hashFiles('flake.lock', 'flake.nix', 'cabal.project', 'ssg/ssg.cabal') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-
          gc-max-store-size-linux: 4G

      - name: Repair restored Nix store
        if: steps.nix-cache.outputs.hit == 'true'
        run: nix-store --verify --repair

      - name: Build site
        run: nix build .#website --no-link --accept-flake-config --print-build-logs
